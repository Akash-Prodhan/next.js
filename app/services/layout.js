export default function ServicesLayout({ children }) {
    return (
        <section>
            <h1>
                welcome in services page
            </h1>
            {children}
        </section>
    );
}